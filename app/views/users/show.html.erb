<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<% @videos.each do |video| %>
  <% @video = Video.find(video.video_id) %>
  <%= @video.name %> 
  year: <%= @video.year %> 
  <br>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
<%= link_to 'Organize videos into sections', select_assign_sections_path %>
